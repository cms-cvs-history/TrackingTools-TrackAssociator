# -*-SH-*-
process TEST = {

   untracked PSet options =
   {
       untracked vstring Rethrow = {"ProductNotFound"}
   }

  service = MessageLogger {
          untracked vstring destinations = {"cout"}
	  untracked PSet cout = {
		        untracked string threshold = "INFO"
		        # string threshold = "DEBUG"
			untracked PSet default = { int32 limit = -1  }
		      }
           untracked vstring debugModules = { "test" }
  }
  
  source = PoolSource {
     untracked vstring fileNames = {
       "file:/uscms_data/d1/jribnik/data/1000/reco_muons1000_1000.root"
     }
     untracked int32 maxEvents = 1000
     # untracked uint32 skipEvents = 250
  }
    
    include "Geometry/CMSCommonData/data/cmsIdealGeometryXML.cfi"
    include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"
    
#   include "Geometry/CMSCommonData/data/cmsSimIdealGeometryXML.cfi"
   include "Geometry/CaloEventSetup/data/CaloGeometry.cfi"
   include "Geometry/CaloEventSetup/data/CaloTopology.cfi"

#   include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
   include "Geometry/DTGeometry/data/dtGeometry.cfi"
   include "Geometry/RPCGeometry/data/rpcGeometry.cfi"
   include "Geometry/CSCGeometry/data/cscGeometry.cfi"
   
   # es_module = TrackerGeometricDetESModule {}

    module trackAssoc = TestTrackAssociator {
      vstring labels = {
      "EBRecHitCollection, ecalRecHit, EcalRecHitsEB",
      "EERecHitCollection, ecalRecHit, EcalRecHitsEE",
      "CaloTowerCollection, towerMaker",
      "DTRecSegment4DCollection, dt4DSegments",
      "CSCSegmentCollection, cscSegments"
      }
      bool useEcal = true
      bool useHcal = true
      bool useMuon = true
    }

    module Out = PoolOutputModule {
      untracked string fileName = "file:/uscms_data/d1/jribnik/data/1000/associated_muons1000_1000.root"
    }
   
    
    path p = {trackAssoc}
    endpath e = {Out}
}

